<Window x:Class="EmailValidatorApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Email Validator" 
        Height="288" 
        Width="496"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border Background="{StaticResource SpotifyGray}" 
            CornerRadius="10" 
            Margin="10">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" 
                              BlurRadius="20" 
                              Color="#000000"/>
        </Border.Effect>

        <Grid Margin="20">
            <!-- Кнопка закрытия -->
            <Button Content="✕" 
                    FontSize="12"
                    Foreground="{StaticResource SpotifySubtext}"
                    Background="Transparent"
                    BorderThickness="0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Width="25"
                    Height="25"
                    Click="CloseButton_Click"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource SpotifyText}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <StackPanel VerticalAlignment="Center">
                <!-- Иконка -->
                <Image 
                    Source="D:\!PROJECTS_TOP\EmailValidator\EmailValidatorApp\Assets\Spotify_Primary_Logo_RGB_Green.png" 
                    Height="47"
                    Width="76" 
                    Margin="0,0,0,10"
                   />




                <TextBox x:Name="EmailBox"
                         Text="test@example.com"
                         Background="#282828"
                         Foreground="{StaticResource SpotifyText}"
                         BorderBrush="#404040"
                   
                         FontSize="16"
                         VerticalContentAlignment="Center"
                         CaretBrush="{StaticResource SpotifyGreen}"
                         SelectionBrush="#404040" Height="40">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Grid>
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}">
                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                              Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                        </Grid>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Content="Проверить"
                        Background="{StaticResource SpotifyGreen}"
                        Foreground="Black"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Height="40"
                        Margin="0,20,0,15"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="ValidateButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="20">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1ED760"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#1AA34A"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Результат -->
                <TextBlock x:Name="ResultText"
                           Text="Введите email для проверки"
                           Foreground="{StaticResource SpotifySubtext}"
                           HorizontalAlignment="Center"
                           FontSize="13"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>